# Shared across dev and prod — copy to .env and fill in.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Environment-specific values (VITE_API_BASE_URL, VITE_DEMO_MODE) live in
# .env.development / .env.production instead — Vite loads the right one
# automatically based on `vite` (dev) vs `vite build` (prod).
